// Rappresenta un stack delle ricerche che mi permette di muovermi
// avanti e indietro tra tutte le pagine visitate e mantenere traccia
// degli spostamenti effettuati dall'utente
import { createBrowserHistory } from 'history';

export default createBrowserHistory();